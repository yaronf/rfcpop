Install WSGI:

sudo apt-get install libapache2-mod-wsgi

Config files:

django.wsgi: in the project directory
settings.py: should be merged with the dev settings.py
rfcpop.conf: under /etc/apache2/conf.d/
